# Built binaries
/aegis
/bin/
cmd/aegis/aegis

# Go build / test artifacts
*.test
*.out
*.prof
coverage.out
coverage.html

# rtmx local cache + verify output (the database.csv itself is tracked — do NOT ignore it)
.rtmx/cache/
rtmx-results.json

# Local audit logs / runtime state (stays in-enclave; never committed)
*.log
/audit/
/run/

# Local-only model/calibration scratch (calibration.json under deploy/ IS tracked)
*.gguf
/models/

# Editor / OS cruft
.DS_Store
.idea/
.vscode/
*.swp
*~

# NOTE: vendor/ is intentionally NOT ignored — offline builds depend on it.
