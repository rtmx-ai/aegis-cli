# RTMX Configuration
# See https://rtmx.ai for documentation

rtmx:
  database: .rtmx/database.csv
  schema: core
  markers:
    rust:
      pattern: '#\[req\("(REQ-[A-Z]+-\d+)"\)\]'
    comment:
      pattern: '// @req (REQ-[A-Z]+-\d+)'
